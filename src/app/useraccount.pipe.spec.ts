import { UseraccountPipe } from './useraccount.pipe';

describe('UseraccountPipe', () => {
  it('create an instance', () => {
    const pipe = new UseraccountPipe();
    expect(pipe).toBeTruthy();
  });
});
